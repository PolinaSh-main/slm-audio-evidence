# Decisions Log / Журнал решений

Format: `date — decision — why`. Newest on top. Entries may be in English or Russian.
Schema changes (PLAN.md §2) MUST be announced here the same day.

- 2026-07-23 — **First `geval_v1.txt` run (Qwen3-8B, no-think) scored WORSE than `judge_v1.txt` (90%/74-82 vs 94%/87-93, 11 new UNPARSEABLE) — root-caused to a token-budget bug, not a G-Eval quality regression.** `geval_v1.txt` asks the model to write its 5 evaluation steps as VISIBLE output (no `<think>` block involved, since `enable_thinking=False`), but `max_new_tokens` still defaulted to 64 -- the value tuned for `judge_v1.txt`'s bare one-word reply (`LocalHFJudge.set_thinking()`'s no-think default). Verified against all 11 UNPARSEABLE raw outputs: every single one is truncated mid-Step-1, all ~250-290 chars (~64 tokens), cut off before ever reaching a verdict word. Separately, of the 8 disagreements, **7 are bare one-word replies** ("INCORRECT"/"CORRECT", no steps at all) -- the model ignored the steps instruction outright on those, behaving identically to `judge_v1.txt` -- so this run barely exercises the G-Eval hypothesis either way; the 90% number is not evidence the explicit-steps approach hurts judgment, just evidence the token budget didn't fit it. Fix: candidate cells now pass an explicit higher `max_new_tokens` whenever `USE_GEVAL_PROMPT` is on (see `notebooks/kaggle_run.ipynb`'s shared setup cell) instead of relying on the thinking-based 64/512 default. Set to **4096** (M4's call, 2026-07-23) rather than a tighter headroom value -- `_StopOnVerdict` never early-stops geval candidates anyway (no `_THINK_CLOSE_MARKERS` in this prompt, thinking is off), so this is purely a ceiling on the model's own EOS-terminated reply, not a forced length; costs nothing extra in the expected case where the model finishes well under it. Re-run needed before drawing any real conclusion about G-Eval vs judge_v1.txt.

- 2026-07-23 — **Финальный вывод по thinking-режиму судьи (Ministral-3-8B-Reasoning, max_new_tokens=4096, оба parse_verdict-фикса применены): 92% (68/74), 19 unparseable, 6 реальных несогласий — точность идентична no-think Ministral-Instruct (92%, 86/93), thinking НЕ точнее.** Реальная цена thinking — не accuracy, а время (9640с на 74 вопроса) и потеря покрытия (19/93 ≈ 20% вопросов остаются вообще без вердикта). Все 6 несогласий однонаправленные (human=INCORRECT, модель=CORRECT, никогда наоборот) и кластеризуются вокруг ОДНОГО паттерна: gold-ответ формулирует абстрактный/обобщающий вывод (два признака вместе, вывод через объяснение, обобщающая формулировка), а response перечисляет много правдивых, тематически близких, но НЕ идентичных деталей, ни разу не проговаривая сам gold-вывод явно. Рассуждение модели в этих случаях само замечает разрыв ("doesn't explicitly state... but if it's implied", "still correct in spirit"), а затем убеждает себя в снисходительности вместо строгой сверки фактов — то есть thinking используется не для более строгой проверки, а для конструирования оправдательного моста между темой и выводом. Отдельно `sq-0229-B3` — не тот же паттерн, а обрыв ответа на самокоррекции "But wait..." + слепота регэкспа к отрицанию ("not fully correct" распознано как CORRECT). Кросс-проверено с literature (Eugene Yan, "Evaluating the Effectiveness of LLM-Evaluators"): это известный общий сдвиг LLM-judge — модели детектируют >95% consistent/верных ответов, но только 30-60% inconsistent/неверных, т.е. асимметричная снисходительность, а не специфика конкретной модели или промпта. **Решение: no-think остаётся основным судьёй по умолчанию** (та же точность, быстрее, без потери покрытия); thinking как основной режим не оправдан. Следующий эксперимент (не блокирует это решение): G-Eval-style промпт с явными пронумерованными evaluation steps (form-filling вместо открытого thinking) — метод специально бьёт в найденный разрыв явным шагом "не засчитывать топически близкий, но не эксплицитный ответ" (см. `src/prompts/geval_v1.txt`, тумблер `USE_GEVAL_PROMPT` в `notebooks/kaggle_run.ipynb`, ячейка shared-setup) — не требует thinking-модели, работает в один проход поверх любого no-think инстанса.

- 2026-07-22 — **`parse_verdict()`: второй, независимый баг парсинга — модель регулярно повторяет вердикт-слово ДВАЖДЫ БЕЗ РАЗДЕЛИТЕЛЯ ("...INCORRECTINCORRECT"), и `\b`-границы слова не видят это как два отдельных вхождения вообще.** Обнаружено при разборе конкретного несогласия (`sq-1628-B1`): в самом тексте модель чётко пишет "the correct response is: INCORRECTINCORRECT" — явный, недвусмысленный вывод INCORRECT, совпадающий с человеческой разметкой. Но зафиксированный вердикт был CORRECT — неправильно. Причина: между двумя склеенными "INCORRECT" нет границы слова (`\b` требует перехода буква/небуква, а между двумя "T" и "I" его нет) — вся конструкция `INCORRECTINCORRECT` для регулярки `_VERDICT_RE` не существует, и фоллбек "последнее слово" вместо этого подхватил случайное более раннее упоминание "correct" (из фразы-шаблона "respond with exactly one word: CORRECT, INCORRECT, or ABSTAINED"). Проверила по всем 93 записям грепом на `\b(CORRECT|INCORRECT|ABSTAINED)(CORRECT|INCORRECT|ABSTAINED)\b` — нашла 9 таких случаев, минимум 2 из которых реально переворачивали итоговый вердикт на неверный. **Фикс**: `_GLUED_VERDICT_RE` — вставляет пробел между двумя вердикт-словами, слипшимися без разделителя, ПЕРЕД основным матчингом; ничего не меняет для уже корректно разделённых случаев (например `"CORRECT.CORRECT"` — там разделитель `.` уже даёт границу). **Итоговый результат после обоих фиксов**: **92% (68/74)**, 19 `UNPARSEABLE`, всего 6 реальных несогласий (было 8 — 2 оказались чистыми артефактами парсинга, не ошибками модели).
- 2026-07-22 — **`parse_verdict()`: добавлен фоллбек "последнее вердикт-слово побеждает", когда маркера конца рассуждения нет ВООБЩЕ.** Прогон Ministral-3-8B-Reasoning с `max_new_tokens=4096` (см. запись выше) всё ещё дал 51/93 `UNPARSEABLE` — проверила не гадая: ни в одном из 51 сырого `raw_output` нет ни `[THINK]`, ни `[/THINK]` вообще (при этом длина ответов 959–2825 символов, далеко не 4096 токенов — значит, дело не в обрыве по лимиту). Модель, вопреки собственному системному промпту (сгенерированному её же `chat_template.jinja`, который явно просит `[THINK]...[/THINK]`), для короткой задачи "ответь одним словом" часто просто рассуждает свободным текстом без тегов вообще. Без маркера — старая логика сканирует весь текст и спотыкается о то же самое (например "...и **correct** calculation..." рядом с финальным "...INCORRECT"). **Фикс**: если маркер не найден вообще (`marker_found=False`) и в тексте есть хоть одно вердикт-слово — берём ПОСЛЕДНЕЕ упоминание как ответ модели, а не требуем ровно одно уникальное на весь текст. Логика та же, что уже работала для `</think>`: ранние упоминания — это перебор гипотез, финальное — реальный вывод; разница только в том, что тут нет явной границы, поэтому используется позиция, а не тег. Если маркер БЫЛ найден, а текст после него всё равно неоднозначен — остаётся честный `UNPARSEABLE` (это уже другой случай: сам финальный ответ модели не определился). Проверено на всех 51 реальных `UNPARSEABLE`-ответах: 31 расшифровался, ручная проверка нескольких (`sq-0525-B2` → INCORRECT, `sq-2499-B2` → ABSTAINED) подтвердила, что последнее слово — действительно заявленный вывод модели. Новый скор для этого кандидата: **89% (65/73)** вместо 88% (37/42) — та же точность, но втрое меньше `UNPARSEABLE` (20 вместо 51).
- 2026-07-22 — **Ministral-3-8B-Reasoning: `max_new_tokens` поднят с 512 (дефолт) до 4096.** После фикса `[/THINK]`-маркера (запись ниже) несколько пунктов всё ещё обрывались на полуслове, не дойдя ни до какого маркера конца рассуждения вообще, — реальный обрыв по бюджету токенов, не баг парсинга. Рассмотрели и ОТКЛОНИЛИ "budget forcing" (принудительно дописать `[/THINK]` + короткий добавочный `generate()`, если модель не уложилась) — решили, что это противоречит собственному принципу проекта "не гадать за неясный вердикт" (`parse_verdict`'s `UNPARSEABLE` контракт): выжимать ответ из модели, которая ещё не пришла к выводу, — это форма угадывания с другой стороны. Простое поднятие потолка честнее: если пункт не укладывается даже в 4096 — это реальный сигнал, а не артефакт тесных рамок. НЕ убрали лимит совсем — детерминированное декодирование (`do_sample=False`) в принципе может зациклиться, а лимит GPU-часов сессии Kaggle общий на всех ~93×N вызовов, один зависший вызов не должен иметь возможность съесть его целиком.
- 2026-07-22 — **Ministral-3-8B-Reasoning (thinking=True) возвращал `UNPARSEABLE` почти на каждом пункте, ~118с/пункт (одинаково, подозрительно) — причина: свой формат тега рассуждения, не `</think>` Qwen3, а `[THINK]...[/THINK]` (квадратные скобки).** M4 сама заметила параллель с уже известным багом Qwen3 (decisions.md 2026-07-15: рассуждение "перебирает" несколько вердиктных слов как гипотезы, если не отсечь текст ДО конца reasoning-блока). Проверила не гадая: скачала реальный `chat_template.jinja` `mistralai/Ministral-3-8B-Reasoning-2512` с Hub — формат `[THINK]...[/THINK]`, задокументирован явно в системном промпте шаблона. `_StopOnVerdict` (`src/judges/local_hf.py`) и `parse_verdict()` (`src/judges/base.py`) были жёстко завязаны на `"</think>"` — маркер Mistral никогда не совпадал, поэтому: (1) `_StopOnVerdict` никогда не останавливал генерацию раньше лимита — отсюда одинаковые ~118с на пункт (всегда 512 токенов до конца); (2) `parse_verdict` сканировал ВЕСЬ текст целиком вместо только финального ответа — рассуждение обычно упоминает "correct"/"incorrect" как обычные слова по ходу анализа (например "...и **correct** calculation" рядом с финальным "INCORRECT") — два разных вердикт-слова → `UNPARSEABLE`, даже когда модель реально пришла к чёткому выводу. **Фикс**: вынесла список маркеров конца reasoning-блока в `_THINK_CLOSE_MARKERS = ("</think>", "[/THINK]")` (`src/judges/base.py`), `parse_verdict` и `_StopOnVerdict` теперь проверяют оба по очереди вместо одного захардкоженного. **Не до конца решено**: несколько пунктов (`sq-4420-B1`, `sq-1122-B1`, `sq-0176-B1`) обрываются на полуслове, ДО `[/THINK]` вообще — это настоящий обрыв по `max_new_tokens=512`, не решается фиксом маркера; reasoning-стиль этой модели выглядит многословнее Qwen3, возможно понадобится поднять `max_new_tokens` именно для этого кандидата (например через `run_candidate("local", {..., "max_new_tokens": 1024}, thinking=True)`).
- 2026-07-22 — **Уточнение к записи ниже: `AutoModelForImageTextToText`-фоллбек был необходим, но недостаточен — реальный чекпоинт Ministral-3 всё равно грузился с "тихо" рандомными весами (LOAD REPORT: все ключи `language_model.*`/`vision_tower.*` — `MISSING`), просто без исключения.** M4 сама предложила верный путь ("что мешает посмотреть нейминг в ЭТОЙ версии и адаптироваться") вместо отката на старый transformers — и это оказалось абсолютно правильно. Проверила напрямую, не гадая: скачала реальный `model.safetensors.index.json` чекпоинта (`language_model.model.layers...`, `language_model.lm_head...` — БЕЗ обёртки `model.` сверху) и сравнила с исходником `modeling_mistral3.py` из двух версий transformers — локальной 4.57.6 (там `Mistral3ForConditionalGeneration._checkpoint_conversion_mapping = {"^language_model.model": "model.language_model", "^vision_tower": "model.vision_tower", "^multi_modal_projector": "model.multi_modal_projector", "^language_model.lm_head": "lm_head"}`) и `main`-ветки на GitHub (соответствует `5.15.0.dev0`, версии M4 после `pip install git+...`, там этот атрибут вообще убран из класса). Применив старый regex к реальным именам вручную, результат ДОСЛОВНО совпал с ключами из отчёта `MISSING` — то есть именно отсутствие переименования и есть причина. `from_pretrained` НЕ бросает исключение на это — тихо возвращает модель со случайными весами вместо ошибки, что опаснее, чем падение: без явной проверки код выглядел бы "успешным" и написал бы в манифест бессмысленный вердикт-судья. **Фикс (`src/judges/local_hf.py`)**: механизм переименования никуда не делся из `from_pretrained` — он всё ещё принимает kwarg `key_mapping: dict[str, str]` (переехал в `conversion_mapping.py`/`core_model_loading.py`, поддерживается на любой версии). Перед загрузкой проверяем `AutoConfig.from_pretrained(model_id).model_type == "mistral3"` и передаём тот же самый мэппинг, что раньше применялся автоматически — работает независимо от версии transformers, не требует отката. Заодно сузила `except ValueError` до случаев с текстом "Unrecognized configuration class" — раньше он слепо перехватывал ЛЮБОЙ `ValueError` и ретраил через `AutoModelForImageTextToText`, включая настоящий OOM/VRAM-конфликт (bitsandbytes `"dispatched on the CPU or the disk"`) — который повторялся один в один на втором Auto-классе, маскируя реальную причину под видом "просто не тот класс".
- 2026-07-22 — **`run_candidate()` (лестница кандидатов) не освобождал VRAM, если что-то падало ДО конца цикла по `RUNS` — из-за этого упавший Ministral-3 "отравил" следующий по счёту Qwen3.5-9B той же ошибкой bitsandbytes, хотя тот сам по себе укладывался в бюджет.** Очистка (`del`/`gc.collect`/`torch.cuda.empty_cache`) раньше стояла ПОСЛЕ основного тела функции — если `build_judge()` или `run_evaluation()` бросали исключение раньше, до неё дело не доходило, и модель оставалась висеть в памяти для следующей ячейки (тот же класс бага, что уже чинили для ячейки 13/14, но здесь оказался незакрыт). **Фикс**: всё тело `run_candidate()` (`notebooks/kaggle_run.ipynb`) обёрнуто в `try/finally` — очистка теперь гарантированно выполняется при любом исходе, `cand_judge = None` в начале страхует случай, когда `build_judge()` падает до создания объекта.
- 2026-07-22 — **Ministral-3-8B (обе ячейки, Instruct и Reasoning) падал с `ValueError: Unrecognized configuration class ... Mistral3Config ... for this kind of AutoModel: AutoModelForCausalLM` — не версия transformers, а неверный Auto-класс.** Сначала подозревала версию (M4 сама попробовала `pip install --upgrade transformers` и `pip install git+https://github.com/huggingface/transformers.git` — не помогло, та же ошибка). Проверила через веб-поиск (не гадаю): `mistral3` официально зарегистрирован в HF `MODEL_FOR_IMAGE_TEXT_TO_TEXT_MAPPING_NAMES` → `Mistral3ForConditionalGeneration`, а НЕ в мэппинге `AutoModelForCausalLM` — это архитектурное решение HF (VLM-класс), не баг конкретной версии, и версия тут ни при чём (источник: github.com/EleutherAI/lm-evaluation-harness#3483, та же проблема). **Фикс**: `src/judges/local_hf.py` — `LocalHFJudge.__init__` теперь пробует `AutoModelForCausalLM.from_pretrained(...)`, и только если это падает с `ValueError`, повторяет попытку через `AutoModelForImageTextToText.from_pretrained(...)` (тот же `kwargs`, включая квантование/`device_map`). Ничего не меняет для уже рабочих чисто текстовых чекпоинтов (Qwen3-8B и т.п.) — только добавляет запасной путь для VLM-классифицированных чекпоинтов вроде Ministral-3.
- 2026-07-22 — **Gemma-4-12B-it (`gemma4_unified`) требует transformers >= 5.5.0 — это НЕ решается тем же фиксом, что Ministral-3, и НЕ решилось версией, которую реально удалось поставить.** Проверила через веб-поиск (не гадаю): `gemma4_unified` добавлен в transformers только с релиза 5.5.0 (источники: github.com/huggingface/optimum-intel#1764, github.com/vllm-project/llm-compressor#2562 — там та же архитектура специально исключена пином `transformers<=4.57.6`). M4 подтвердила на практике: даже `pip install git+https://github.com/huggingface/transformers.git` (ветка main) даёт версию `5.0.0` — этого всё ещё недостаточно (`5.0.0 < 5.5.0`), ошибка та же. Не устранено — это реальный, а не мнимый (кэш модуля/тёплый kernel) блокер: `requirements.txt` пинит `transformers>=4.44` без верхней границы, а обновление до `>=5.5.0` — это мажорный скачок (4.x → 5.x), не проверенный на остальном пайплайне (Qwen3-8B, `BitsAndBytesConfig`, `run_eval.py`) в той же kernel-сессии. Решение по Gemma-4 (пробовать `transformers>=5.5.0` в полностью отдельной Kaggle-сессии только под эту модель, или выкинуть её из лестницы кандидатов на этот раз) — за M4, не форсирую сама.
- 2026-07-22 — **Лестница судей-кандидатов (`notebooks/kaggle_run.ipynb`) разбита из одного цикла по списку `CANDIDATES` на отдельную ячейку на каждую модель — по просьбе M4, для более удобного запуска.** Один общий цикл означал: нельзя перезапустить один упавший кандидат (например Gemini после квоты, см. запись про `flash-lite` ниже), не перезапустив остальных; нельзя пропустить/отложить кандидата (например VLM-модели до проверки класса загрузки) без правки списка. Рефакторинг: общая ячейка-сетап (импорты, `SAVE_FLAT_CACHE`, `_slugify`, `save_flat_and_upload`, `free_disk_cache_for`, новая `run_candidate(backend, kwargs, thinking, free_disk_cache=True)`) выполняется один раз, дальше — по ячейке на модель, каждая просто вызывает `run_candidate(...)`. Манифест (`results/judge_audit_multi/run_manifest.json`, читает `audit_multi_judge.py`) больше не собирается один раз в конце цикла в памяти — `_append_manifest_entry()` дописывает/заменяет (по `judge_name`) запись на диске после каждого кандидата, переживает падение любой последующей ячейки и не дублирует запись при повторном запуске той же модели. `free_disk_cache` параметризован (по умолчанию `True`, `False` для первого из двух прогонов `Qwen3.5-9B` — no-think и thinking, чтобы второй не качал те же веса заново).
- 2026-07-22 — **Уточнение к записи ниже: смена модели (`flash` → `flash-lite`) не решила `ResourceExhausted` — настоящая причина не в модели, а в неверифицированном Google Cloud проекте.** После переключения на `gemini-2.5-flash-lite` та же ошибка повторилась на 4-м вызове с **тем же `limit: 20`**, что и раньше на `gemini-2.5-flash` — совпадение лимита для двух разных моделей было подсказкой, что дело не в RPM/RPD конкретной модели. Проверила через веб-поиск (не гадаю): `generate_content_free_tier_requests` — это урезанный бакет для API-ключей без привязанного биллинга в Google Cloud Console, срабатывает ОДИНАКОВО для любой модели Gemini на таком ключе (источники: https://www.technetexperts.com/gemini-api-429-limit-0-quota-fix/, GitHub issues `google-gemini/gemini-cli#22486`/`#13112` — та же проблема у других). Пауза между вызовами и разные модели тут бессильны в принципе. **Реальный фикс — не код**: привязать карту в Billing соответствующего GCP-проекта (это не тратит деньги, просто снимает ключ с урезанного бакета) и перевыпустить API-ключ (старый может остаться в старом бакете), обновить `GEMINI_API_KEY` в Kaggle Secrets. Пауза/честное ожидание в `GeminiJudge._generate()` оставлены как есть — не мешают и пригодятся после фикса при настоящем RPM-лимите, но сами по себе эту проблему не решают.
- 2026-07-22 — **`GeminiJudge` переключён с `gemini-2.5-flash` на `gemini-2.5-flash-lite` — реальный `ResourceExhausted` на Kaggle (проект планирует ~1000 вызовов судьи за прогон, пилот+scale-set), а не гадание.** С заработавшим фиксом ошибки судьи (см. запись про `raw_output` ниже) впервые стала видна настоящая причина `FAILED` у Gemini: `ResourceExhausted... Quota exceeded... limit: 20, model: gemini-2.5-flash`. Проверила актуальные free-tier лимиты (не с потолка — https://ai.google.dev/gemini-api/docs/rate-limits, https://www.aifreeapi.com/en/posts/gemini-api-free-tier-rate-limits): `gemini-2.5-flash` — 10 RPM/**250 RPD**, `gemini-2.5-flash-lite` — 15 RPM/**1000 RPD**, `gemini-2.0-flash` — 5 RPM и вдобавок снят с поддержки в 2026. Только `flash-lite` реально покрывает дневной объём этого проекта. **Рассмотрела и ОТКЛОНИЛА идею фоллбека на другую модель Gemini при исчерпании квоты** (была прямая просьба M4, затем сама же и отменена: "если у нас в одном датасете будут ответы от разных моделек, это пиздец") — вердикты от двух разных judge-моделей внутри одного `results/` каталога несравнимы между собой, это хуже, чем один последовательный (пусть чуть более слабый) судья. Вместо фоллбека: (1) `MODEL_ID` в `src/judges/gemini.py` = `gemini-2.5-flash-lite` дефолтно, (2) `GeminiJudge` теперь **проактивно** выдерживает паузу между вызовами (`60/15` с, под реальный RPM), чтобы не упираться в лимит вообще, а не только реагировать на него постфактум, (3) если 429 всё же случится — парсит точное время ожидания из самого сообщения Google ("Please retry in 55.4s") вместо слишком короткого `2**attempt`-бэкоффа (1/2/4 с, заведомо недостаточно против per-minute/per-day лимита). `CANDIDATES` в `notebooks/kaggle_run.ipynb` обновлён на `gemini-2.5-flash-lite`.
- 2026-07-22 — **Ячейка судьи падала с `ValueError: Some modules are dispatched on the CPU or the disk` при повторном запуске в тёплом kernel'е (например, чтобы сменить `JUDGE_MODE`/`JUDGE_PROMPT`) — не связано с самим переключением флага.** `judge = build_judge(...)` строит НОВЫЙ инстанс до того, как имя `judge` перепривязывается — старая модель из предыдущего запуска (~9 ГБ VRAM, int8) оставалась живой в момент загрузки второй копии, на GPU не хватало места, и `device_map="auto"` пытался вынести часть слоёв на CPU/диск — bitsandbytes 8-bit это запрещает и падает именно этой формулировкой (не обычным `CUDA OOM`, что и сбивало с толку — выглядело как будто виноват `JUDGE_MODE="full"`). **Фикс**: в начале ячейки судьи (`notebooks/kaggle_run.ipynb`, перед `judge = build_judge(...)`) добавлена очистка `if "judge" in dir(): del judge; gc.collect(); torch.cuda.empty_cache()` — теперь повторный запуск ячейки в той же сессии безопасен независимо от того, что именно меняется (`JUDGE_MODE`, `JUDGE_PROMPT`, `JUDGE_THINKING`).
- 2026-07-22 — **Путь монтирования Kaggle Dataset на аккаунте M4 на уровень глубже, чем документация подразумевает: `/kaggle/input/datasets/<логин>/<slug>`, а не `/kaggle/input/<slug>`.** Обнаружено при первой реальной попытке использовать плоский кэш (см. запись выше): `FLAT_MODEL_DIR = "/kaggle/input/qwen3-8b-flat-cache"` (путь, который я изначально предположила по общей практике Kaggle) не сработал — `os.path.isdir()` вернул `False`, ячейка тихо ушла в ветку "качаем с нуля" вместо явной ошибки. Диагностировано командой `!ls -la /kaggle/input/datasets/*/` (M4 запустила по запросу, не гадали) — реальная структура `/kaggle/input/datasets/pasheviakova/qwen3-8b-flat-cache/`. Похоже, это тот же формат, что уже был виден в самом первом логе кэш-проблемы этого дня ("Reused cached weights from `/kaggle/input/datasets/pasheviakova/qwen3-cache/hf_cache`") — этот сигнал я тогда пропустила, стоило сопоставить раньше. **Фикс**: `FLAT_MODEL_DIR` в `notebooks/kaggle_run.ipynb` (ячейка судьи) поправлен на правильный вложенный путь; `save_flat_and_upload()` в цикле кандидатов и одноразовая утилита для Qwen3-8B теперь САМИ печатают точный ожидаемый путь монтирования после загрузки (`/kaggle/input/datasets/<логин>/<slug>`) вместо того, чтобы полагаться на пользователя/меня угадать его заново для каждой новой модели.
- 2026-07-22 — **Сырой HF-кэш как Kaggle Dataset потерял `snapshots/` при загрузке (M4, реальный прогон) — переход на "плоский" кэш (`save_pretrained`) + `kagglehub.dataset_upload`, минуя `/kaggle/working` вообще.** Диагностика: `find /root/hf_cache` показал `blobs/` (реальные 16 ГБ байт есть) и `refs/`, но НЕ показал `snapshots/` — а именно `snapshots/<rev>/config.json -> ../../blobs/<hash>` (символическая ссылка) единственно связывает бессмысленные хэш-имена блобов с настоящими именами файлов. Без неё `from_pretrained` не может найти НИЧЕГО, даже при полностью рабочей сети (`curl -sI huggingface.co` вернул 200) — отсюда `OSError: ... couldn't find them in the cached files` несмотря на все 16 ГБ на диске. Причина: Kaggle Datasets ненадёжно сохраняют symlink'и при загрузке — формат кэша HF (blobs+ссылки) в принципе плохо переживает путь "Dataset -> распаковка". `pasheviakova/qwen3-cache` — известный битый экземпляр этого, использовать не нужно.
  **Первая версия фикса (сохранить как обычные файлы в `/kaggle/working`) тоже оказалась неполной** — M4 верно указала: `/kaggle/working` — это Output-квота ~19 ГБ, а часть моделей лестницы (`Qwen3.6-27B` ~54 ГБ) в неё не влезает даже в теории, вне зависимости от формата кэша. Решение (после веб-поиска, не с потолка): библиотека `kagglehub` (`kagglehub.dataset_upload(handle, local_dir)`, https://github.com/Kaggle/kagglehub) грузит датасет с ЛЮБОГО локального пути кодом, минуя кнопку "New Dataset" интерфейса (та привязана именно к Output) — лимит датасета 100 ГБ, а не 19 (https://www.kaggle.com/getting-started/228867), авторизация внутри Kaggle-ноутбука уже есть, ничего настраивать не нужно. Итоговая схема: `model.save_pretrained()`/`tokenizer.save_pretrained()` в `/root/qwen3_flat` (большой локальный диск, без quota) -> `kagglehub.dataset_upload(...)` оттуда же. Ничего вручную кликать не нужно. См. новую утилитную ячейку в `notebooks/kaggle_run.ipynb` сразу после ячейки судьи.
- 2026-07-22 — **Kaggle Output-квота (~19 ГБ) переполнилась на первой же модели-судье; `HF_CACHE_DIR` переехал с `/kaggle/working` на `/root/hf_cache`.** Обнаружено на практике (M4): `Qwen/Qwen3-8B` (~16 ГБ) под `/kaggle/working/hf_cache` (Output ноутбука, жёсткий лимит) сам почти забил квоту — лестница кандидатов ниже (`Ministral-3-8B`, `Qwen3.5-9B`, `Qwen3.6-27B` — по 15-55 ГБ каждая) не поместилась бы вообще. Фикс: `HF_CACHE_DIR = "/root/hf_cache"` (отдельный, гораздо больший локальный диск контейнера, не персистится между сессиями и не должен — важны только `results/`). В цикле кандидатов (`notebooks/kaggle_run.ipynb`) добавлена очистка диска после каждой локальной модели (`huggingface_hub.scan_cache_dir` + `delete_revisions`), кроме случая, когда тот же `model_id` нужен ещё раз позже в списке (`Qwen3.5-9B` встречается дважды — no-think/thinking).
  **Побочная находка**: `HF_HOME`/`HF_HUB_OFFLINE` читаются `huggingface_hub` ОДИН РАЗ, при первом импорте в процессе — правка env-переменных в уже тёплом kernel'е (где `transformers` уже импортирован из предыдущего прогона) молча игнорируется: кэш скопирован, `HF_HUB_OFFLINE=1` выставлен, а `from_pretrained` всё равно реально качает веса заново (не просто дергает Hub за метаданными — настоящая скачка, подтверждено логом с реальной скоростью в МБ/с). Единственный фикс — `Run -> Restart session` и заново сверху (ячейки 2, 3, 4, затем судья) — ничем другим уже импортированную библиотеку не заставить перечитать переменные среды. Добавлена явная проверка `du -sh` сразу после копирования кэша, чтобы видеть, действительно ли восстановление отработало, до того как это выяснится косвенно через попытку скачивания.
  **`HF_HUB_OFFLINE=1` откачен в тот же день.** После чистого рестарта (см. выше) `du -sh` подтвердил полные 16 ГБ на диске, но `from_pretrained` упал с `OSError: We couldn't connect to huggingface.co ... couldn't find them in the cached files` — то есть форсированный оффлайн-режим сам стал причиной падения. До правки (самое первое сообщение M4 про кэш) `from_pretrained` БЕЗ `HF_HUB_OFFLINE` уже работал правильно: короткая проверка ревизии на Hub (пара КБ — "Fetching N files"/"Download complete: 0.00/0.00", НЕ повторная закачка весов), затем переиспользование локального кэша без скачивания. Оффлайн-режим требует, чтобы кэш был полностью самодостаточен (корректные `refs/`-указатели и т.п.) без права на сетевую подстраховку — скопированный через `cp -r` кэш оказался недостаточно точным для этого строгого пути, хотя обычному онлайн-пути хватало за глаза. Вывод: `HF_HUB_OFFLINE` не форсируем — экономия в пару КБ трафика не стоит хрупкости; сама переезд-фикс (`/root/hf_cache` вместо `/kaggle/working/hf_cache`) остаётся в силе, он решал реальную (дисковую) проблему.
- 2026-07-21 — **Проверены на huggingface.co реальные `model_id` для судей-кандидатов из лестницы (были плейсхолдерами с дня ресерча) — две правки и одна новая находка.** (1) Ранее записанный "Mistral-3-8B" не существует под этим именем: реальная линейка называется **Ministral-3** — `mistralai/Ministral-3-8B-Instruct-2512-BF16` (без переключателя thinking) и **отдельный** чекпоинт `mistralai/Ministral-3-8B-Reasoning-2512` (всегда рассуждает, тоже без переключателя — как DeepSeek-R1-Distill). (2) Правильные id для Qwen: `Qwen/Qwen3.5-9B` и `Qwen/Qwen3.6-27B` (без суффикса `-Instruct` — сами модели уже чатовые, как и наш `Qwen3-8B`); оба Apache 2.0, оба подтверждают `enable_thinking` в `apply_chat_template`. (3) **Находка**: все три новых кандидата (`Ministral-3-8B`, `Qwen3.5-9B`, `Qwen3.6-27B`) — vision-language модели, не чисто текстовые; `LocalHFJudge` грузит через `AutoModelForCausalLM` — совместимость этого класса с VLM-чекпоинтами в text-only режиме НЕ проверена (нет доступа к GPU/HF-загрузке из этой сессии). Не гадаю и не чиню вслепую — в `notebooks/kaggle_run.ipynb` добавлена рекомендация загрузить каждую новую модель отдельно перед полным прогоном цикла и явно смотреть на ошибку класса; если она есть, это пробел в `LocalHFJudge`, который нужно закрывать отдельно, а не предположение, которое можно тихо оставить непроверенным.
- 2026-07-21 — **Инфраструктура для сравнения нескольких моделей-судей (продолжение задачи 1 ROLE_M4).** Добавлено вместо ручного повторения `audit_judge.py compare` на каждую модель. `src/run_eval.py`/`run_evaluation()` не менялся — уже поддерживал произвольный `out_dir` на кандидата, этим и пользуемся: `notebooks/kaggle_run.ipynb` получил новый цикл (после замороженной ячейки Qwen3-8B, которую не трогали), гоняющий список кандидатов (`CANDIDATES`) ОДНИМ процессом, с выгрузкой GPU-памяти (`del` + `torch.cuda.empty_cache()`) между `local`-моделями — та же защита от OOM, что и раньше (см. запись 07-16). Каждый кандидат пишет в свою `results/<run_id>/llm_audit_<judge.name>/`, никогда не трогая `responses_judged.jsonl` (manual-M1). Цикл также пишет `results/judge_audit_multi/run_manifest.json` (judge_name, model_id, backend, thinking, время в секундах на кандидата).
  `scripts/audit_judge.py`: логика сравнения (`cmd_compare`) вынесена в `compute_agreement()` — тот же результат для CLI-команды `compare`, плюс переиспользуется новым скриптом ниже вместо копипасты (проверено: `compare` даёт тот же % согласия, что и раньше, регрессии нет).
  Новый `scripts/audit_multi_judge.py`: читает `run_manifest.json`, для каждого кандидата считает agreement vs manual-M1 и пишет `results/judge_audit_multi/<judge_name>/compare_manual_m1.md` + `disagreements.jsonl` (какие именно вопросы провалены, полный текст) — отдельно на каждую модель. Если у кандидата `thinking: true` в манифесте — ПОЛНЫЙ raw-вывод судьи (включая `<think>...</think>`) для КАЖДОГО провопрошенного вопроса (не только провалов) пишется в `results/judge_audit_multi/thinking_traces/<judge_name>/` — отдельно от сравнительных таблиц по явному требованию (не мешать reasoning-трейсы с тем, что команда читает в первую очередь). Одна `comparison.md`/`.csv` сверху — все кандидаты в одной таблице, отсортированы по убыванию согласия, с колонками thinking (да/нет) и время. Проверено smoke-тестом на двух `FakeJudge`-кандидатах (один no-think, один с фейковым `<think>` блоком) — вся цепочка (цикл → манифест → сравнение → трейсы → таблица) отработала корректно на болванках, реальные модели ещё не гонялись (нужен сервер/Kaggle).
- 2026-07-21 — **Судейская ветка `m3/llm-judge` переделана: чистое авторство + смёржен новый `main` (пивот к фазе статьи); старый PR #1 обнаружен закрытым M1.** По задаче 1 из `ROLE_M4.md`. Все 23 коммита ветки были авторизованы как `Polina <nerinaperevinkl@gmail.com>` (личный email, не для публикации) — переписаны на `Polina Shevyakova <pasheviakova@gmail.com>` через `git filter-branch --env-filter` (диапазон `a85506c..cc81377`, только свои коммиты, ничего чужого не тронуто). Локальный git-конфиг репозитория (не глобальный) выставлен на ту же личность. После рерайта смёржен обновившийся `origin/main` (пивот к фазе статьи 18.07, AQUA-Bench снят с роли валидации 19.07) — три конфликта (`README.md`, `README_RU.md`, `docs/decisions.md`), все текстовые (append-only лог + одна строка про платформу инференса), разрешены вручную, тесты (`test_judges.py`, `test_local_hf.py`, `test_tiered.py`) зелёные после мёржа. Ветка запушена force-with-lease в `PolinaSh-main/slm-audio-evidence-judge-fork` (форк, из которого открывался PR #1) — **`gh pr view` показал PR #1 уже закрыт M1 (`ladnlav`) 18.07**, ещё до этого рерайта; значит "переделка" по задаче 1 подразумевает новый PR, не реюз старого. **Не закрыто**: задача 1 требует ещё "протестировать дополнительные модели-судьи" + таблицу аудита (модель × согласие с 93 gold B) в описании PR — `GeminiJudge` (`src/judges/gemini.py`) уже реализован, но НИ РАЗУ не прогонялся против золотых меток (только `local`/Qwen3-8B аудирован пять раз, см. записи ниже). PR не открываю, пока таблицы нет — этого явно требует "Готово, когда" в ROLE_M4.md.
- 2026-07-19 — **AQUA-Bench снят с роли валидационного сета** (находка M2, проверено по официальной странице бенчмарка): состав — только звуки животных, неречевые вокализации и музыкальные инструменты, речевого контента нет; каскад на неречевом аудио не определён, а наше утверждение — про речевое содержание. AQUA остаётся опорой позиционирования в Related Work. **Замена: скейл-сет на нативном SQuAD 2.0** — ~200 безответных (`is_impossible=true`) + ~200 отвечаемых вопросов, написанных людьми, поверх уже приматченного Spoken-SQuAD-аудио; плюс NMSQA-разведка (test-часть NMSQA — те же SQuAD-параграфы, начитанные 60 живыми дикторами): при ≥ ~100 C на натуральном аудио скейл-сет получает натуральный слайс (ответ на критику «TTS-артефакт»). Новые значения схемы (PLAN §2): `source: nmsqa`, `generator: native-squad2`, `subtype: native-unanswerable`. Пилот-100 остаётся замороженным диагностическим набором. LibriSpeech и прочие натуральные корпуса без готовых вопросов отклонены (вернули бы ручную генерацию+верификацию) → Future Work. Часть записи от 18.07 про валидацию на AQUA-Bench — устарела.
- 2026-07-18 — **Пивот к фазе статьи.** Единственный дедлайн: статья через OpenReview до вс 02.08 21:00 МСК (шаблон «Записок ПОМИ», ≤10 стр + приложения, не анонимно, все авторы на OpenReview, обязательная секция Contribution). Ниша: датасет + оценка + **митигация**; бенчмаркинг неопределённости — территория параллельной офлайн-команды. Новый план: [en/PLAN.md](en/PLAN.md) / [ru/PLAN.md](ru/PLAN.md).
- 2026-07-18 — **Стержень фазы 2** — перенос pre-generation attention probing (arXiv 2606.21917, статья 24) на Qwen2-Audio; митигация probe→abstain против базлайнов plain/S1/энтропия. План-минимум без пробинга зафиксирован (PLAN §5); решение о переключении — чекпоинт пт 24.07; гибкий стоп экспериментов — ср 29.07.
- 2026-07-18 — **Объём стратегий и владение A2:** в фазе статьи гоняем только plain и S1 — ось сравнения «промпт vs сигнал на выходе vs сигнал до генерации»; обзор промпт-стратегий уже сделан в ст. 02, повторять не вклад. S4 переехал внутрь метода (те же k сэмплов → мягкие метки и энтропия), S5 — опциональный базлайн, S2 — только как MCQ-формат AQUA, S3 вне фазы. Стержень A2 ведёт M1 целиком (дизайн, код снятия, зонды, анализ); вычислительные прогоны на DataSphere (снятие, сэмплирование) — M2; оценка сэмплов и протокол CV — M4; страховка: при недоступности M1 линейные зонды обучает M2 по его дизайн-записи.
- 2026-07-18 — **Датасет-стратегия:** пилот-100 остаётся замороженным *диагностическим набором*, расширения не будет; масштаб добавляется валидацией на AQUA-Bench (~200–400 элементов, обе системы × plain/S1). Формат (MCQ/free-form) — решение M1 от 20.07 по предложению M2.
- 2026-07-18 — **Доки радикально пересмотрены:** PLAN/PROPOSAL/ROLE_M1–M3 (en+ru) предзащиты → `docs/archive/predefense/`; новые короткие доки: PLAN, ROLE_M1–M4, PAPER_OUTLINE (en+ru). GLOSSARY остался живым, дополнен терминами пробинга. Канонические схемы данных сохранили адрес PLAN.md §2.
- 2026-07-18 — **Инфраструктура фазы 2:** основная площадка — Яндекс DataSphere (грант 5 000 000 юнитов), Kaggle у M4 — запасная. В papers/ добавлены статьи 24–27 (probing 2606.21917, Walking Through Uncertainty 2604.25591, LISTEN 2505.14518, BALSa 2505.20166); чтение по PLAN §6 — стартует 18–19.07, до сих пор статьи не читал никто.
- 2026-07-16 — **Ad-hoc ячейка `judge_v5.txt` упала с CUDA OOM на Kaggle (14.56 ГиБ GPU) — грузила вторую копию Qwen3-8B поверх уже загруженной в основной ячейке; исправлено переиспользованием инстанса.** Моя же ошибка: `tiered.py` специально спроектирован не грузить модель дважды (см. его докстринг), но при добавлении ad-hoc ячейки для `judge_v5.txt` я об этом забыл и написал `build_judge("local", ...)` заново — два int8 8B в VRAM одновременно не влезли. **Фикс**: `LLMJudge.set_prompt(prompt_name)` (`src/judges/base.py`) — меняет `prompt_name`/`prompt_version` на уже существующем инстансе, без пересоздания. `LocalHFJudge.set_thinking(enable_thinking, max_new_tokens=None)` (`src/judges/local_hf.py`) — то же самое для режима генерации, и **заодно** держит `.name` (компонент ключа кэша) в согласии с реальным режимом — раньше при простом `setattr(judge, "enable_thinking", ...)` имя судьи "-v1"/"-v2" могло разъехаться с тем, что реально исполняется, и незаметно испортить кэш. `TieredJudge.judge()` тоже переведён на `set_thinking()` вместо ручного `setattr` по словарю `_FAST_MODE`/`_SLOW_MODE` (убраны) — тот же метод, меньше дублирования. Ad-hoc ячейка в ноутбуках теперь: `adhoc_backend = judge.judge_backend if hasattr(judge, "judge_backend") else judge` (разворачивает `TieredJudge`, если вдруг выбран этот режим) `→ .set_prompt("judge_v5.txt") → .set_thinking(True)` — тот же объект, та же загруженная модель, ноль дополнительной VRAM. Покрыто юнit-тестами (`test_set_prompt_reuses_instance` в `test_judges.py`, `test_set_thinking` в `test_local_hf.py` — тестирует именно логику метода на болванке, без реальной загрузки модели) и smoke-тестом полного ноутбучного потока (fake backend, проверка `is judge` — тот же объект). `judge_v5.txt`+thinking+Apollo 13 ещё не прогонялся успешно ни разу — следующий шаг на Kaggle.

- 2026-07-16 — **`judge_v4.txt` (v1 без транскрипта) на полном датасете: 90% (84/93), было 94% — гипотеза "транскрипт мёртвый груз" ОПРОВЕРГНУТА, откатили дефолт ноутбука обратно на `judge_v1.txt`.** `eighth_kaggle_run_no_transcript_with_baseline_and_one_thinking_question_GITIGNORE/` (только тест 1 — full dataset, no-think; данных по точечному тесту `judge_v5.txt`+thinking на `sq-5207-B2` в этой папке нет). Хотя `{transcript}` действительно не упомянут в написанном критерии вердикта (см. запись выше), уборка его из промпта стоила −4 п.п.: 1 случай починился (`sq-2005-B3`), но **5 сломались** — `sq-1122-B1`, `sq-1443-B1`, `sq-4398-B1`(qwen2audio_plain), `sq-5092-B1`, `sq-0338-B2`. Все пять — короткие, легитимные, но НЕ дословные ответы (пример: `sq-1122-B1` — "Secondary sources of European Union law are based on the treaties." — верно по сути, короче gold — без транскрипта в контексте судья резче их бракует). **Вывод**: транскрипт, хоть и не цитируется явно в инструкции, реально работает как стабилизирующий контекст, который удерживает судью от чрезмерной строгости к кратким парафразам — implicit use, не только "занимает токены". Урок для следующих гипотез про промпт: то, что модель не ССЫЛАЕТСЯ на что-то в тексте критерия, не значит, что она это не использует — нужно измерять, не выводить из чтения инструкции. **Откат**: `notebooks/*.ipynb` — `JUDGE_PROMPT`/`JUDGE_MODE` дефолт возвращён на `judge_v1.txt`/`dev_subset`. `judge_v4.txt` остаётся в репозитории как задокументированный отклонённый эксперимент (не удалён), `DEFAULT_PROMPT_NAME` не менялся, был и остаётся `judge_v1.txt`.

- 2026-07-16 — **Два новых эксперимента подготовлены (ещё не прогнаны на Kaggle): `judge_v4.txt` (v1 без транскрипта, полный датасет) и `judge_v5.txt` (v3 без транскрипта, thinking, только Apollo 13).** Мотивация: `{transcript}` в `judge_v1.txt` подан в промпт, но ни разу не упомянут в критерии вердикта — по факту не используется по написанным инструкциям (см. запись выше), только жжёт токены на prefill. `judge_v4.txt` = `judge_v1.txt` минус строка `TRANSCRIPT: {transcript}`, иначе байт-в-байт идентичен (`src/prompts/judge_v4.txt`). Тест — `JUDGE_MODE="full"`, `JUDGE_THINKING=False`, теперь дефолт в обоих ноутбуках: если точность не просядет против 94% (87/93) — транскрипт можно выкинуть насовсем, чистая экономия. `judge_v5.txt` = `judge_v3.txt` минус та же строка, инструкция "if the system answer states a specific fact that is absent from the transcript..." в тексте оставлена как есть (`src/prompts/judge_v5.txt`). Целевая гипотеза — точечная: в седьмом прогоне (`judge_v3.txt`+thinking, полный dev_subset) рассуждение по `sq-5207-B2` (Apollo 13) обрывалось на середине верной мысли из-за `max_new_tokens=512`; без токенов на транскрипт рассуждение может стать короче и уложиться в бюджет. Тестируется отдельной ad-hoc ячейкой в конце ноутбука (`subset_ids={"sq-5207-B2"}`, все 4 прогона — вопрос в каждом маршрутизируется на судью) — сознательно не через весь `dev_subset`, чтобы не тратить время на нерелевантные вопросы. Прогонять v5 с транскриптом отсутствующим, но инструкция всё равно ссылается на "the transcript" — намеренно оставлено так по запросу, а не переписано под "no transcript given"; интересно посмотреть, как модель обработает инструкцию про источник, которого физически нет в контексте. Обе версии + новый JUDGE_PROMPT/JUDGE_MODE дефолт покрыты юнит-тестами (`tests/test_judges.py::test_prompt_versioning`) и smoke-тестом на fake backend по реальным `results/`.

- 2026-07-16 — **`judge_v3.txt` + thinking на dev_subset: последний запланированный эксперимент провален, судья ЗАМОРОЖЕН на no-think `judge_v1.txt` (94%, 87/93).** `seventh_kaggle_run_judge_small_subset_NEW_prompt_THINKING_GITIGNORE/`. 7 из 23 (30%) — UNPARSEABLE, хуже, чем в чистом v1+thinking (10/93≈11%) — усложнённая инструкция v3 (сверить ещё и с транскриптом, не только с gold) удлиняет рассуждение, чаще упирается в `max_new_tokens=512` до вердикта. **Главная цель, `sq-5207-B2` (Apollo 13) — по-прежнему не решена**, но теперь по другой причине: `raw_output` обрывается на полуслове ровно в момент, когда судья, похоже, находит нужное расхождение — *"...the gold answer's main point is that Apollo 13 didn't land because of the explosion. So, the system answer includes"* — и здесь текст обрывается (`</think>` не закрылся). Рассуждение шло в верном направлении, но не уложилось в бюджет. Среди 16 распарсенных — 81% (13/16), то есть по чистой точности не лучше, чем v3 без thinking (83%), а по покрытию — сильно хуже (56% против 100%). Плюс новое расхождение: `sq-5092-B2` (TARDIS) — human correct, судья INCORRECT, ответ не выглядит галлюцинацией (перефразирует "blue police box... common sight" без выдуманных фактов) — похоже на тот же паттерн, что `sq-3203-B3` в прошлой записи (путает "не покрывает gold дословно" с "выдумал факт"). **Итог всей серии экспериментов** (v1 no-think 94% baseline → v2 no-think 89% → v1 thinking хуже базы → v3 no-think 83% смешанно → v3 thinking 81%/56% покрытия, хуже всех): ни один рычаг — ни промпт, ни thinking, ни их комбинация — не побил простой no-think `judge_v1.txt`. **Решение**: прекращаем итерацию над судьёй здесь. Рабочий конфиг — `judge_v1.txt`, `enable_thinking=False` (уже дефолт в ноутбуке: `JUDGE_THINKING=False`, `JUDGE_PROMPT="judge_v1.txt"`). Оставшиеся ~3 случая (`sq-5207-B2` точно; `sq-3555-B2`/`sq-5092-B2` — возможные пропущенные галлюцинации, где human, вероятно, не применял hallucination-focused критерий, см. записи от 2026-07-15) фиксируются как задокументированный, принятый предел single-pass 8B judge на этом наборе рубрик — не как открытый баг. `judge_v2.txt`/`judge_v3.txt` остаются в репозитории как задокументированные, отклонённые эксперименты (не удаляю — история важна), `DEFAULT_PROMPT_NAME` остаётся `judge_v1.txt`.

- 2026-07-15 — **`judge_v3.txt` + no-think на dev_subset: 83% (19/23), было 78% на v1 — чистый +1, но с новым классом false positive, деплоить рано.** `sixth_kaggle_run_judge_small_subset_NEW_prompt_GITIGNORE/`. Исправилось 3 из 5 старых расхождений: `sq-2571-B1` (ASR-опечатка), `sq-2005-B3` и `sq-4398-B1` (оба — не пойманные раньше галлюцинации, ровно то, на что целился фикс). Сломалось 2 новых: `sq-3203-B3` — **настоящий false positive**, ответ дословно эхо транскрипта ("Public schools funded by provincial government. Private schools funded by private sector."), ни одного выдуманного факта, просто короче gold (не упоминает "businesses and sponsors") — судья всё равно сказал INCORRECT, похоже спутал "не покрывает gold полностью" с "содержит факт не из транскрипта". `sq-3555-B2` (dioxygen) — вероятно, судья тут прав, а не human: ответ действительно добавляет "21% of the atmosphere by volume", "supports photosynthesis", "marine animals breathe through gills" — ничего этого в транскрипте нет, это настоящая галлюцинация по критерию из `sq-4398-B1` (задача — отвечать из транскрипта), просто human её не отследил. **Главная цель фикса, `sq-5207-B2` (Apollo 13, оба прогона) — НЕ исправилась.** raw_output — голое `"CORRECT"`, без единого слова рассуждения: no-think (max_new_tokens=64, 1 forward pass) физически не даёт модели место сверить конкретные факты ответа с транскриптом — предсказание из предыдущей записи подтвердилось. **Решение (по запросу команды)**: следующий и последний запланированный эксперимент в этой серии — `judge_v3.txt` + `JUDGE_THINKING=True` на `dev_subset` (в ноутбуке уже есть оба флага, поменять `JUDGE_PROMPT="judge_v3.txt"`, `JUDGE_THINKING=True`, `JUDGE_MODE` оставить `"dev_subset"`). Если и это не даст чистого улучшения без новых false positive — останавливаемся на no-think `judge_v1.txt` (94%, см. запись выше) и фиксируем оставшиеся ~3 случая (`sq-5207-B2`, `sq-3555-B2`/если подтвердится и т.п.) как задокументированный предел single-pass LLM-judge, а не как баг, который нужно чинить дальше.

- 2026-07-15 — **Контрольный прогон на `JUDGE_MODE="dev_subset"` (no-think `judge_v1.txt`): 78% (18/23), ниже 80%-гейта — это ложная тревога, не регресс.** `fifth_kaggle_run_judge_small_subset_old_prompt_GITIGNORE/` — первая реальная проверка после сегодняшних правок разметки (`sq-3647-B2`, `sq-3032-B2`) и нового флага `JUDGE_THINKING`. Оба фикса подтвердились **вживую** (не только на пересчёте кэша): `responses_judged.jsonl` этого прогона несёт уже исправленный `gold_answer` и `correct: true` для обоих id. `scripts/audit_judge.py compare` напечатал "BELOW 80% GATE" — ожидаемо: `dev_subset` намеренно на 13 из 23 (57%) состоит из заведомо сложных id (объединение всех расхождений прошлых аудитов, см. `src/judges/dev_subset.py`), тогда как в полном пилоте сложных ~7-13%. Порог `AGREEMENT_GATE=0.80` в `audit_judge.py` калиброван под представительную выборку (протокол ROLE_M3 Task 4), а не под сознательно adversarial сабсет — предупреждение технически верное, но вводит в заблуждение вне контекста. Все 5 расхождений — уже известные, не новые: `sq-2571-B1` (ASR-опечатка, отложено), `sq-2005-B3`, `sq-5207-B2`×2, `sq-4398-B1` (все три — подтверждённые ранее реальные слепые пятна судьи, не разметки, см. записи выше). **Следующий шаг**: тот же `dev_subset`, но `JUDGE_PROMPT="judge_v3.txt"` — ещё не прогонялся.

- 2026-07-15 — **`JUDGE_MODE="tiered"` снят с рекомендации; no-think `judge_v1.txt` пересчитан на 94% (87/93) бесплатно после починки разметки; ноутбук получил `JUDGE_THINKING`.** Каскад (`tiered.py`) был построен на гипотезе "thinking чинит verbosity-bias у длинных ответов" — четвёртый аудит (thinking включён) её не подтвердил: ни один из известных сложных случаев (`sq-5207-B2`, `sq-2005-B3`, `sq-4398-B1`) не выиграл от рассуждения, thinking проиграл no-think по чистой точности на всём датасете (75/93 vs 86/93, см. запись от 2026-07-15 выше "Честное сравнение"). Каскад не удаляю (код рабочий, протестирован), но он больше не JUDGE_MODE по умолчанию и не рекомендуется гонять на Kaggle, пока thinking не докажет пользу хоть для одного реального случая. Заодно пересчитала no-think `judge_v1.txt` (второй прогон, кэш уже на диске, GPU не нужен) против исправленных сегодня лейблов (`sq-3032-B2`, `sq-3647-B2`): **94% (87/93)**, было 92% (86/93) — `sq-3032-B2` вышел из расхождений (лейбл поправлен), `sq-3647-B2` пока висит в старом кэше как расхождение (ключ кэша не учитывает правки `gold_answer`, только смену файла промпта — свежий прогон с тем же `judge_cache.jsonl` тихо вернёт устаревший вердикт; **новый** audit-каталог посчитает верно, т.к. `run_evaluation` берёт gold из манифеста заново). Ноутбуки (`notebooks/*.ipynb`, ячейка судьи): добавлен флаг `JUDGE_THINKING` (по умолчанию `False`) — раньше `JUDGE_MODE="full"`/`"dev_subset"` включали thinking неявно (дефолт `LocalHFJudge(enable_thinking=True)`), не было способа явно выбрать лучший подтверждённый конфиг одной галочкой. `JUDGE_MODE` по умолчанию сменён на `"dev_subset"` (дешёвая проверка `judge_v3.txt` — следующий реальный шаг). Проверено: юнит-тесты + smoke-тест ветвления `JUDGE_MODE`×`JUDGE_THINKING` → `(judge_backend, judge_kwargs)` и полного цикла на fake-backend по реальным `results/`.

- 2026-07-15 — **Ручной разбор "неубиваемых" расхождений (3/3 и 2/3 аудитов) — не всё одной природы, проверила каждый случай по транскрипту.** Три вопроса были неверно оценены ЛЮБОЙ конфигурацией судьи, испробованной до сих пор (модель, промпт v1/v2, thinking вкл/выкл): `sq-3647-B2`, `sq-5207-B2`, `sq-4398-B1`. Разобрала каждый вручную (транскрипт + полный ответ, не по агрегатам):
  1. **`sq-3647-B2` (иммунитет) — баг разметки, не судьи.** `gold_answer` был "It is antigen-specific and can generate responses tailored to specific pathogens or infected cells." — это ОДНА мысль (антиген-специфичность), пересказанная двумя фразами; транскрипт же прямо называет ДВА признака ("antigen-specific... tailored responses" **и отдельно** "immunological memory" / memory cells). Модель во всех инстансах отвечала "specificity + memory" — точнее по транскрипту, чем сам gold. **Исправлено**: `data/manifests/pilot.jsonl` — `gold_answer` для `sq-3647-B2` переписан на "It is antigen-specific, generating responses tailored to specific pathogens or infected cells, and it relies on immunological memory (memory cells) to respond quickly if the same pathogen infects the body again." Синхронизировала копии `gold_answer` во всех 4 `results/*/responses_judged.jsonl` (там же поле дублируется). `correct` не трогала — human уже везде говорил correct, это теперь просто соответствует исправленному gold.
  2. **`sq-3032-B2` (школы, cascade_plain) — тоже баг разметки.** Ответ дословно перечисляет все 5 категорий gold (parochial/католические, протестантские, еврейские, мусульманские, православные) плюс структуру "две группы", которая тоже читается из транскрипта. Другой инстанс той же модели с почти идентичным по качеству ответом (`cascade_s1_idk_20260712`) уже был размечен `correct: true` — несостыковка с этим конкретным инстансом похожа на разовую невнимательность при разметке, не системную проблему. **Исправлено**: `results/cascade_plain_20260712/responses_judged.jsonl` — `correct: false` → `true` для `sq-3032-B2`. Пересчитан `metrics.md` того же прогона через `calculate_all_metrics`/`generate_markdown_report` (`src/metrics.py`) на обновлённом `responses_judged.jsonl`: точность на категории B 90.0% → 93.3% (28/30), остальные метрики (A, C, refusal) не менялись, т.к. это единственная затронутая строка.
  3. **`sq-5207-B2` (Apollo 13) и `sq-2005-B3` (Лютер) — настоящие ошибки судьи, не разметки.** В обоих случаях ответ модели вставляет конкретную деталь, которой физически нет в транскрипте (Apollo 13: "loss of cabin pressure" — транскрипт говорит про "disabled propulsion and life support", ни слова о давлении в кабине; Лютер: "power over the Jews... Jewish temple and sacrifices" — этого нет в транскрипте вообще), и при этом теряет точную формулировку gold. Судья (во всех конфигурациях) не заметил галлюцинацию — купился на уверенный, топически-релевантный тон. Human прав, gold в порядке. **Механическая причина**: `{transcript}` присутствует в промпте, но ни одна инструкция рубрики не просит модель сверять `{response}` с транскриптом на предмет непокрытых конкретных фактов — инструкции просят сравнивать `{response}` только с `{gold}` ("same meaning as the gold answer"). Транскрипт технически в контексте, но не используется как проверочный якорь. **Кандидат-фикс подготовлен, НЕ задеплоен**: `src/prompts/judge_v3.txt` — минимальный (одно предложение) diff от v1, как и v2 в своё время: *"If the system answer states a specific fact that is absent from the transcript, treat that as a hallucination and mark the answer INCORRECT even if it also contains the gold answer's facts."* **Явная оценка риска перед тестом**: это качественно более сложная задача, чем анти-verbosity фраза в v2 (которая уже провалилась, 92%→89%) — "факт отсутствует в транскрипте" требует сверки на уровне конкретных утверждений, а не поверхностного сигнала типа длины; модель может как начать банить легитимные парафразы (типична для этой модели за-строгая реакция на условные правила), так и не научиться ловить галлюцинации вообще. Ожидаю повышенный риск отката по сравнению с v2. **Тестировать только в изоляции**: `JUDGE_PROMPT="judge_v3.txt"` + `JUDGE_MODE="dev_subset"` на известных случаях (`sq-5207-B2`×2, `sq-2005-B3`, плюс "лёгкие" контрольные из `dev_subset.py` — чтобы сразу увидеть, если v3 сломает легитимные парафразы) прежде чем расширять. `DEFAULT_PROMPT_NAME` остаётся `judge_v1.txt`.
  4. **`sq-4398-B1` (фармацевты) — human-разметка уже правильная по критерию "не из транскрипта = галлюцинация", ничего чинить не пришлось.** Явное решение команды: раз вся работа — про галлюцинации аудио-LLM, а задача — отвечать по транскрипту, то ответ с верной сутью, но обёрнутый в придуманные детали, должен быть INCORRECT. Проверила все 4 инстанса против короткого транскрипта (5 предложений, называет только: optimize medication use, promote health/wellness/disease prevention, collaborate with physicians, patient care rounds/drug product selection): `cascade_plain` и `cascade_s1_idk` — ответы полностью укладываются в то, что реально сказано в транскрипте (`correct: true`, уже верно). `qwen2audio_plain` и `qwen2audio_s1_idk` — ответы добавляют MTM, скрининги (давление/холестерин/глюкоза), прививки, консультации по отказу от курения и т.п. — ничего из этого в транскрипте нет (`correct: false`, уже верно). Никаких правок не потребовалось — human изначально разметил все 4 инстанса ровно так, как требует hallucination-focused критерий, ещё до того как мы его явно сформулировали. Хороший знак по качеству manual-M1. Судейские же вердикты (не human) как раз и путали `qwen2audio_plain`/`qwen2audio_s1_idk` инстансы на CORRECT — та же корневая причина, что и в пункте 3 (не сверяет с транскриптом), фиксится (если выйдет) тем же `judge_v3.txt`.

- 2026-07-15 — **Честное сравнение до/после thinking (после фикса `parse_verdict` выше) — thinking не выигрывает; открытый вопрос про облегчённое рассуждение без native `<think>`.** По всем 93 элементам, не только по распарсенной части: до thinking (second run, no-think `judge_v1.txt`) — 86/93 = 92% верно, 7 неверно, 0 без ответа. После thinking (fourth run, тот же `judge_v1.txt`, `enable_thinking=True`) — 75/93 = **81%** верно (не 90% — та цифра была только по 83 распарсенным), 8 неверно, 10 без ответа (обрыв на `max_new_tokens=512`, `</think>` не закрылся). Построчное сравнение (union расхождений обеих версий, 11 id): 1 исправилось (`sq-2571-B1`, ASR-опечатка — та самая, которую сознательно не пытались чинить), 4 сломалось (`sq-0005-B2`, `sq-3647-B2`/qwen2audio_plain, `sq-4398-B1`/qwen2audio_plain, `sq-5092-B1`/qwen2audio_plain), 4 стабильно неверны что с thinking, что без (`sq-3032-B2`, `sq-2005-B3`, `sq-5207-B2`×2 — те же verbosity-случаи, что и до), плюс 8 ранее верных элементов ушли в UNPARSEABLE (чистая потеря покрытия, не ошибка). Вывод: native thinking (`enable_thinking=True`) сам по себе не решил verbosity-bias на этих конкретных сложных вопросах и стоил части покрытия. **Открытый вопрос (не тестировано)**: увеличить `max_new_tokens` при `enable_thinking=False`, не трогая промпт — прогноз: скорее всего no-op, `judge_v1.txt` прямо просит "reply with exactly one word", без инструкции рассуждать модель не станет использовать лишний бюджет сама. Единственный вероятный способ получить ограниченное (не native, не подверженное обрыву/multi-hypothesis-багу) рассуждение — короткая инструкция в промпте вида "сначала одной фразой отметь, есть ли в ответе факты из gold, потом вердикт одним словом", но это правка промпта, которую делать без явного решения команды не стали (см. принцип "промпт минимален" выше). Нужен реальный прогон на Kaggle, чтобы проверить любую из этих гипотез — предсказание из первых принципов, не измерение.
- 2026-07-15 — **Критический баг найден и исправлен: `parse_verdict` ложно браковал 60/93 (65%) вердиктов thinking-судьи как UNPARSEABLE** — четвёртый аудит (Kaggle, `judge_v1.txt`, thinking включён, `fourth_kaggle_run_judge_better_context_old_prompt_GITIGNORE/`, до троттлинг-оптимизаций из записи ниже) первым дал реальный прогон thinking-режима на полном пилоте. `parse_verdict` сканирует ВЕСЬ ответ судьи и требует ровно одно уникальное вердикт-слово — рассчитано на короткие ответы без thinking. С `enable_thinking=True` рассуждение естественно называет по имени и CORRECT, и INCORRECT как гипотезы ("is this correct or incorrect? let's check...") прежде чем дать один чёткий финальный ответ после `</think>` — из-за этого 59 из 60 UNPARSEABLE-случаев на самом деле имели однозначный ответ, который просто заслонялся собственным рассуждением модели. **Фикс**: `parse_verdict` (`src/judges/base.py`) теперь сканирует на вердикт-слова только текст ПОСЛЕ последнего `</think>`, если он есть; без thinking-блока поведение не меняется. Проверено на реальных данных (не только юнит-тестами): пересчитала все 93 закешированных `raw_output` из четвёртого аудита старым и новым парсером — UNPARSEABLE упал с 60 до 10 (оставшиеся 10 — честная обрезка на `max_new_tokens=512`, `</think>` так и не закрылся, там действительно нет ответа). Официальный отчёт через `scripts/audit_judge.py compare` на исправленных кэшах: **90% (75/83)**, вместо прежних бессмысленных "94% (31/33)" (та цифра считалась по трети датасета, остальное молча терялось). Новые расхождения после фикса — знакомые verbosity-bias случаи (`sq-3032-B2`, `sq-5092-B1`, `sq-2005-B3`, `sq-5207-B2`, `sq-4398-B1` ×2): thinking сам по себе НЕ гарантированно чинит verbosity-bias на этих конкретных вопросах, несмотря на бюджет на рассуждение. Юнит-тесты добавлены в `PARSE_CASES` (`tests/test_judges.py`) на все три случая (гипотезы в thinking, закрытый think без ответа, незакрытый think). **Следующий шаг**: прогнать каскад (`JUDGE_MODE="tiered"`) с исправленным парсером на Kaggle — предыдущие измерения throughput-оптимизаций (запись ниже) делались ДО этого фикса, реальный эффект каскада ещё не проверен на живых данных.
- 2026-07-15 — **Судья: throughput для 1000+ примеров × несколько прогонов** — thinking-режим (см. запись ниже) чинит verbosity-bias, но 20-40+ минут даже на ~90-элементном пилоте не масштабируется на основную фазу. Три меры, не трогая уже провалидированный `judge_v1.txt`: (1) **dev-сабсет** (`src/judges/dev_subset.py`, 23 пары run_id+id — 13 "сложных" — объединение всех disagreement-кейсов из всех прошлых аудитов — плюс 10 случайно выбранных "лёгких", на которых судья и человек ни разу не расходились) для проверки промпта/модели за 1-2 минуты вместо полного прогона; `run_evaluation(..., subset_ids=...)` фильтрует ответы перед оценкой, ничего не льёт поверх основных файлов. Это ортогонально исходному предложению команды: сабсет ускоряет *итерацию* над судьёй, а не финальный прогон на всём датасете — для него нужны отдельные меры. (2) **Умный стоп-критерий** (`_StopOnVerdict` в `local_hf.py`) — генерация останавливается сама, как только после `</think>` появляется вердикт-слово, вместо того чтобы всегда тянуться до `max_new_tokens=512`; не режет сам thinking (до `</think>` работает как раньше), поэтому не мог сломать точность, только время. (3) **Каскад/tiered-судья** (`src/judges/tiered.py`, `TieredJudge`, backend `"tiered"` в `build_judge()`) — быстрый no-think проход (`enable_thinking=False`, ~64 токена) на 100% данных, эскалация на медленный thinking-проход только если ответ ≥3× длиннее gold по словам (ровно verbosity-bias паттерн, единственная известная причина расхождений с человеком). Один загруженный `LocalHFJudge` переиспользуется для обеих скоростей через переключение `enable_thinking`/`max_new_tokens` между вызовами — намеренно не грузим модель дважды (два int8 8B в VRAM одновременно не влезли бы в 16 ГБ T4). `LocalHFJudge.__init__` получил параметр `enable_thinking` (раньше было жёстко `True`), имя судьи теперь `-v1`/`-v2` в зависимости от режима, как и раньше. Ноутбуки: флаг `JUDGE_MODE` (`"tiered"` по умолчанию / `"dev_subset"` / `"full"`) в ячейке судьи. Протестировано: юнит-тесты (`tests/test_local_hf.py`, `tests/test_tiered.py`, `tests/test_judges.py::test_run_eval_subset_ids`) и функциональный smoke-тест ноутбучной ячейки на реальных `results/` через fake-backend (не на Kaggle). **Не измерено на реальных данных**: точность/скорость каскада на Qwen3-8B — следующий шаг на Kaggle.
- 2026-07-15 — **`judge_v2.txt` отклонён (третий аудит, Kaggle, Qwen3-8B): 89% (83/93), было 92% на v1** — минимальная (одно предложение) правка всё равно вызвала непредсказуемую регрессию, как и предупреждала команда. Матрица: ложных CORRECT (судья прощает неверный ответ) стало меньше — 5→2, ровно то, на что целился фикс. Но ложных INCORRECT (судья бракует верный ответ) стало больше — 2→8. Конкретные новые жертвы содержат нужные факты из gold дословно (`sq-4398-B1`/cascade_plain: ответ явно перечисляет "Optimizing Medication Use / Promoting Health and Wellness / Disease Prevention" — точное совпадение с gold — и всё равно INCORRECT; аналогично `sq-3555-B2` dioxygen, `sq-5092-B2` TARDIS). Причина: при `max_new_tokens=64` и выключенном thinking у судьи физически нет бюджета сверить факты, поэтому добавленная фраза про verbosity превратилась в паттерн-матчинг "длинный ответ → подозрительно" вместо реальной проверки условия "…только если там нет фактов". **Решение**: откатить рубрику обратно на `judge_v1.txt` (промпт больше не трогаем), вместо этого дать судье реальное место на рассуждение — `LocalHFJudge` теперь генерирует с `enable_thinking=True` (родной `<think>` Qwen3) и `max_new_tokens=512` (было 64); `judge_name` → `llm-qwen3-8b-v2`, чтобы кеш не путал старые no-think вердикты с новыми (ключ кеша уже включает `judge_name`, ничего доп. чинить не пришлось). Оба ноутбука обновлены (`JUDGE_PROMPT` только `judge_v1.txt`, markdown объясняет отказ от v2 и более медленный прогон). Ещё не тестировано на Kaggle — следующий шаг.
- 2026-07-15 — **`judge_v2.txt` переписан: минимальный, а не развёрнутый, diff от v1** — команда: длинный промпт сам по себе источник непредсказуемых ошибок, держим рубрику как можно ближе к оригиналу. Убрала более многословный черновик (broadened ABSTAINED, шаг рассуждения перед вердиктом) — теперь v2 отличается от v1 ровно одним добавленным предложением в конце: *"A long or confident-sounding answer that is missing the gold answer's specific facts is still INCORRECT, not CORRECT."* Целится только в подтверждённый verbosity-паттерн (5 оставшихся расхождений после смены модели). Два других найденных случая — сознательно НЕ в промпте: `sq-3647-B2` (immune system) считаем, вероятно, ошибкой ручной разметки, не багом судьи, ждём подтверждения от команды прежде чем что-то менять; `sq-2571-B1` (ASR-опечатка "at sum. Point") — отдельная будущая задача (научить судью читать сквозь артефакты транскрипции), не в этом заходе. `LocalHFJudge.max_new_tokens=64` не откатывала — v2 больше не просит шаг рассуждения, но лишний запас токенов безвреден. Следующий шаг: прогнать v2 (минимальный) поверх Qwen3-8B на Kaggle.
- 2026-07-15 — **Второй аудит судьи (Kaggle, Qwen3-8B, промпт всё ещё v1): 92% (86/93), было 88% (82/93) на Qwen2.5** — смена модели изолированно (без правки промпта) дала +4 нетто. Разобрала id-в-id со старым списком расхождений (`second_kaggle_run_judge_better_Qwen_GITIGNORE/`, не в git): **исправлено 6 из 11** старых, включая оба самых тревожных случая — замаскированные отказы (`sq-1122-B2`, `sq-0005-B1`, *"I'm sorry, I cannot provide an answer..."*), которые Qwen2.5 засчитывал как CORRECT, теперь корректно отклонены. Ещё 4 verbosity-случая тоже исправились (`sq-2499-B2`, `sq-1122-B1`, `sq-5092-B1`, `sq-0338-B2`). **Осталось 5** старых расхождений — тот же паттерн (длинный беглый ответ без конкретных фактов gold получает CORRECT): `sq-3032-B2`, `sq-2005-B3`, `sq-5207-B2` (оба прогона), `sq-4398-B1` — именно то, что должен адресовать `judge_v2.txt` (ещё не тестировался поверх Qwen3). **Появилось 2 новых** расхождения (Qwen3 местами строже человека): `sq-3647-B2` (ответ добавляет неподтверждённую деталь "immunological memory" — судья пометил INCORRECT, человек — correct; спорно, возможно судья прав насчёт частичной галлюцинации) и `sq-2571-B1` (ответ содержит явный артефакт ASR — *"was at sum. Point subject"* вместо "at some point" — судья не смог прочитать смысл сквозь испорченную фразировку, человек смог). **Следующий шаг (не сделан, по решению команды)**: наложить `judge_v2.txt` поверх Qwen3-8B — целится именно в оставшиеся 5 verbosity-случаев.
- 2026-07-15 — **Судья: Qwen2.5-7B-Instruct → Qwen3-8B, промпт пока v1** — намеренно один эксперимент за раз: judge_v2.txt (см. запись ниже) и смена модели решают один и тот же verbosity-bias, но прогнав оба сразу нельзя понять, что помогло. Выбрали сначала проверить модель одну, с промптом v1. `Qwen/Qwen3-8B` подтверждён поиском (не суффикс `-Instruct` — у Qwen3 это чат-модель по умолчанию; `-Base` отдельно). Qwen3 по умолчанию генерирует `<think>...</think>` — передаём `enable_thinking=False` в `apply_chat_template` (проверено локально на реальных токенайзерах Qwen2.5 и Qwen3: на Qwen2.5 — молча игнорируется, на Qwen3 — реально убирает thinking). `do_sample=False` (жадное декодирование) оставлен вопреки рекомендованным Qwen3 sampling-параметрам — воспроизводимость вердиктов важнее. Следующий шаг после прогона на Kaggle: если модель одна не вытащит с 88% дальше — пробовать judge_v2.txt поверх неё.
- 2026-07-15 — **Первый реальный аудит LLM-судьи (Kaggle, Qwen2.5-7B-Instruct): 88% (82/93), но с чётким паттерном ошибок** — 9 из 11 расхождений это судья говорит CORRECT на длинный, бегло звучащий, но фактически пустой (или прямо отказной: *"I'm sorry, I cannot provide an answer as there is no audio provided..."*, `sq-1122-B2`/`sq-0005-B1`) ответ модели. Похоже на verbosity/fluency bias — известная слабость LLM-judge, особенно у моделей поменьше. Разбор: `first_kaggle_run_judge_GITIGNORE/` (локально, не в git) + `scripts/audit_judge.py compare` теперь также пишет `disagreements.jsonl` и секцию `## Disagreements` в отчёт — оба вердикта рядом, без повторного прогона на Kaggle. **Правки в ответ**: (1) `src/prompts/judge_v2.txt` — явно запрещает засчитывать CORRECT за беглость/длину, расширяет ABSTAINED на "просит ещё информацию / извиняется без ответа", просит модель сначала одной фразой сверить факты и только потом дать вердикт (`LocalHFJudge.max_new_tokens` поднят 16→64 под это). Not yet the default — нужна повторная валидация на Kaggle прежде чем менять `DEFAULT_PROMPT_NAME`. (2) Исправлен скрытый баг: `PROMPT_VERSION` был захардкожен как имя файла, а не хэш содержимого — правка текста рубрики "на месте" молча не инвалидировала бы кеш. Теперь `LLMJudge.prompt_version` = `<файл>@<sha256[:8] содержимого>`, `run_eval.py --judge-prompt` выбирает файл рубрики за раз. (3) Модель-судья (`Qwen2.5-7B-Instruct`) выбрана в 2026-07-15 записи ниже была чистым переиспользованием конфига `cascade.py`, НЕ оценкой качества как судьи — открытый вопрос, брать ли модель новее (например Qwen3), пока не решено.
- 2026-07-15 — **LLM-судья работает пока через fork, не PR**: аккаунт, с которого коммитится (`PolinaSh-main`), не в Collaborators `ladnlav/slm-audio-evidence` (push → 403; токен `gh` с scope `repo` тоже не помогает — это repo-level permission, не scope токена). Форкнула в `github.com/PolinaSh-main/slm-audio-evidence`, вся работа из этой записи и записи ниже (`src/judges/`, `scripts/audit_judge.py`, `tests/test_judges.py`, оба ноутбука) лежит там в ветке `m3/llm-judge`. Ноутбуки временно клонируют `REPO_URL`/`REPO_BRANCH` = форк/`m3/llm-judge` (ячейка 2, тот же флаговый паттерн, что `RUN_INFERENCE`/`USE_LOCAL_ZIP`) — **PR в `ladnlav/slm-audio-evidence` намеренно не открыт**, сначала прогон на Kaggle должен подтвердить, что всё работает. После подтверждения: PR + вернуть `REPO_URL`/`REPO_BRANCH` на `origin`/`main`.
- 2026-07-15 — **Compute-платформа: Colab → Kaggle** для `notebooks/` (переименован `colab_run.ipynb` → `kaggle_run.ipynb`, `google.colab.files.download` заменён на Kaggle Output tab, `Runtime →` меню — на Kaggle Settings/Session restart). Заодно инференс-часть (аудио-загрузка + 4 прогона) спрятана за флагом `RUN_INFERENCE` (default `False`): раз `responses.jsonl` и ручная разметка `manual-M1` уже закоммичены в `results/`, ноутбук по умолчанию сразу идёт к прогону и аудиту LLM-судьи, для которых `pilot_audio.zip` не нужен (судья читает только текст из манифеста). Прежний путь (пересчитать инференс с нуля) сохранён под флагом, аудио тогда — через Kaggle Dataset (`AUDIO_ZIP_PATH`), а не upload-панель. **Открыто**: остальные доки (PLAN.md, ROLE_M1/M3, GLOSSARY, PROPOSAL) всё ещё называют Colab как платформу пилота — это исторически верно (пилот 12.07 гонялся на Colab) и не переписывалось; правкам подлежат только новые прогоны основной фазы.
- 2026-07-15 — **LLM-судья реализован** (`src/judges/`, ROLE_M3 Task 1 stage 3): отдельный пакет с `LLMJudge` ABC (по образцу `SpeechQAModel` в `src/models/base.py`) — рендеринг рубрики и парсинг вердикта общие и не зависят от backend'а. Backends: `local` (Qwen2.5-7B-Instruct, int8, тот же конфиг что уже проверен в `cascade.py` на Colab T4 — **основной**, open-source-first), `gemini` (API fallback, требует `GEMINI_API_KEY`, только если локально не влезает). `run_eval.py --judge {none,local,gemini}` — по умолчанию `none`, старое поведение (`correct=None`, `judge="pending-manual"` для B) не меняется. Судья вызывается только для категории B с `label="answer"` — намеренно развязано с rule-based классификатором в `judge.py` (тот не тронут), чтобы смена одного не требовала трогать другое. Вердикты кешируются в `results/<run_id>/judge_cache.jsonl` (ключ: id+judge_name+prompt_version) — повторный прогон не платит за LLM-вызовы повторно. Провенанс в `responses_judged.jsonl.judge`: `"rules"` / `"pending-manual"` / `"llm-<model>-v1"`. Валидация — `scripts/audit_judge.py sample|score`, реализует протокол ROLE_M3 Task 4 (слепая выборка 30 + перепроверка 10, порог согласия 80%). **Открыто**: сам выбор модели-судьи (Qwen2.5-7B-Instruct как дефолт local) и бюджет на аудит — подтвердить с командой перед тем как публиковать LLM-judge числа на слайды.
- 2026-07-13 — `results/**/responses.jsonl` и `responses_judged.jsonl` теперь коммитятся (whitelist в .gitignore): файлы маленькие, прогоны недетерминированы, а ручные оценки B (judge: manual-M1) невоспроизводимы — терять нельзя. Крупные артефакты прогонов остаются в ignore.
- 2026-07-12 — **Каскад вернулся в пилот**: прогоны успели пройти в Colab, поэтому предзащита показывает 4 прогона (2 системы × 2 промпта); запись «каскад перенесён в основную фазу» ниже — устарела. Итоги: `results/pilot_summary.md`.
- 2026-07-12 — Категория B размечена вручную M1 (93 ответа, по полным текстам, `judge: "manual-M1"`); в `judge.py` для неразмеченных B correctness = None и accuracy_B считается только по размеченным; добавлена метрика `no_abstain_rate_on_c`; `run_eval.py` переведён на argparse.
- 2026-07-12 — **Пилот заморожен**: 100 элементов (30 A / 30 B / 40 C: 12 absent-entity + 12 missing-attribute + 8 false-presupposition + 8 off-topic); согласие проверяющих на 20 общих строках = 16/20 (80%); 4 спорных строки авто-дропнуты (sq-0714-B2, sq-3032-C7, sq-0774-B3, sq-3234-C7). `pilot.jsonl` дальше только append-only.
- 2026-07-12 — Каскад (Whisper→LLM) перенесён в основную фазу; прогоны предзащиты = Qwen2-Audio × {plain, s1_idk}.
- 2026-07-10 — До предзащиты коммитим прямо в main без PR (мало времени, команда из 3); правило PR+ревью возвращается в основной фазе.
- 2026-07-10 — LLM-судья на пилоте НЕ используется: категорию B размечаем вручную втроём (~120 ответов); выбор модели-судьи — решение основной фазы. Correctness для B до ручной разметки = None (не 0!).
- 2026-07-10 — Выходная схема прогонов дополнена полем `asr_transcript` (только для cascade) — нужно M3 для sanity-проверки транскриптов.
- 2026-07-08 — Canonical data schemas live in [PLAN.md §2](en/PLAN.md); role files hold convenience copies — one source of truth for field names.
- 2026-07-08 — Literature PDFs are local-only (gitignored); reading guide with sources stays in [papers/README.md](../papers/README.md) — keeps clones small (~70 MB saved).
- 2026-07-08 — Project docs unified and moved into this repo (README/PROPOSAL/PLAN/GLOSSARY ×2 languages + roles/); superseded drafts remain outside the repo in the SMILES folder `archive/` — single living home for docs.
- 2026-07-08 — Repo skeleton created per [PLAN.md §6](en/PLAN.md): data/manifests, src/models, src/prompts, configs, notebooks, results, docs.
